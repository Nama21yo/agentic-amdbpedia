# Base URLs for the two backends this frontend calls. No auth headers are
# sent yet (deferred deliberately for this internal-tool MVP — see README).
PUBLIC_AGENTIC_DBPEDIA_URL=http://localhost:8000
PUBLIC_CROSS_LINGUAL_URL=http://localhost:8001
