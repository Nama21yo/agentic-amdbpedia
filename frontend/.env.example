# Base URL for the cross-lingual HTTP API this frontend calls -- the only
# backend it talks to. No auth headers are sent yet (deferred deliberately
# for this internal-tool MVP — see README).
PUBLIC_CROSS_LINGUAL_URL=http://localhost:8001
