import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from './button.svelte';

export {
	Root,
	type ButtonProps as Props,
	buttonVariants,
	Root as Button,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
};
